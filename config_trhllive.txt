#if the auctioneer will be run via cgi, set the following to a non-zero value
webbased_auctioneer=0

perl_loc=/usr/bin/perl
sh_loc=/bin/sh

league_name=Three Run Homer League
messageboard_url=http://trhl.doorstop.net/phorum/list.php?f=4
